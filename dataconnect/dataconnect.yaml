specVersion: "v1"
serviceId: "swapin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "swapin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
